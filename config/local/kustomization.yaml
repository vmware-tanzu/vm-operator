# Labels to add to all resources and selectors.
#commonLabels:
#  someName: someValue

bases:
- ../etcd
- ../rbac
- ../apiserver
- ../manager
- ../crds/external-crds


patchesJson6902:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: vmoperator-controller
    path: local_replicas_patch.yaml


