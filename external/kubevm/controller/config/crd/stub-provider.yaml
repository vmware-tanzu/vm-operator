# © Broadcom. All Rights Reserved.
# The term "Broadcom" refers to Broadcom Inc. and/or its subsidiaries.
# SPDX-License-Identifier: Apache-2.0
#
# A stub provider CRD for this module's envtest environment. The core is
# duck-typed: it never imports a provider's Go types, so its own test double
# only needs to carry the linkage annotation and the fixed contract status
# paths, not a real provider's spec.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: stubvirtualmachines.infrastructure.stub.kube-vm.io
spec:
  group: infrastructure.stub.kube-vm.io
  names:
    kind: StubVirtualMachine
    listKind: StubVirtualMachineList
    plural: stubvirtualmachines
    singular: stubvirtualmachine
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
          properties:
            spec:
              type: object
              x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
              properties:
                addresses:
                  type: array
                  items:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                powerState:
                  type: string
                providerID:
                  type: string
                providerMetadata:
                  type: object
                  additionalProperties:
                    type: string
                conditions:
                  type: array
                  items:
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
