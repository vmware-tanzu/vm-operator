---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: requiredduringexecutionvmplacementpolicies.vsphere.policy.vmware.com
spec:
  group: vsphere.policy.vmware.com
  names:
    kind: RequiredDuringExecutionVMPlacementPolicy
    listKind: RequiredDuringExecutionVMPlacementPolicyList
    plural: requiredduringexecutionvmplacementpolicies
    singular: requiredduringexecutionvmplacementpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RequiredDuringExecutionVMPlacementPolicy is the schema for the
          RequiredDuringExecutionVMPlacementPolicy API.

          The presence of RequiredDuringExecutionVMPlacementPolicy in a
          namespace allows VM workloads to be eligible for
          requiredDuringExecution Affinity and Anti-Affinity Policies.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              RequiredDuringExecutionVMPlacementPolicySpec defines the desired state of
              RequiredDuringExecutionVMPlacementPolicy.
            properties:
              description:
                description: Description specifies the desired description of the
                  policy.
                type: string
            type: object
        type: object
    served: true
    storage: true
