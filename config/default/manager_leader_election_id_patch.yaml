apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        env:
        - name: LEADER_ELECTION_ID
          value: LEADER_ELECTION_ID_PLACEHOLDER-runtime
